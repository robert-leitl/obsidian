uniform vec2        uResolution;
uniform float       uTime;

in vec2 vUv;
in vec3 vPosition;
in vec3 vNormal;
in vec3 vSurfaceToView;

out vec4 outColor;


void main(void) {
    outColor = vec4(20.);
}